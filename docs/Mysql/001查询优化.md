第一章 查询语句优化
====

1.1 避免使用全表扫描
----
  `【规则1-1.1】` 避免在 where 子句中使用!=或<>操作符。
  `【规则1-1.2】` 避免在where字句里判断''或者null，可以设置默认值为0，判断是否=0。
  ```sql
  select id from t where num is null
  select id from t where num = ''
  可用
  select id from t where num = 0 
  
  ```
  
